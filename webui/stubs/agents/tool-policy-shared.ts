export function normalizeToolName(name: string): string {
  return name;
}

export function resolveToolProfilePolicy(): Record<string, unknown> {
  return {};
}

export function expandToolGroups(): unknown[] {
  return [];
}
