// Package nudge 提供记忆审查（Memory Nudge）功能。
//
// Memory Nudge 系统定期（每 N 轮对话）通过 LLM 审查对话内容，
// 自动将重要信息写入 MEMORY.md 文件，用于跨会话保留用户偏好、
// 角色信息、项目约束等关键信息。
//
// 设计原则：
//   - 非阻塞：审查在 goroutine 中异步执行，不阻塞用户响应
//   - 增量写入：每天一个日期分组，避免覆盖旧内容
//   - 去重检查：写作前检查内容是否已存在
package nudge
